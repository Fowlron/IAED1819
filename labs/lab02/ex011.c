#include <stdio.h>

int main()
{
    int x = 0;
    int y = 0;
    scanf("%d", &x);
    scanf("%d", &y);

    if (x > y && x > y)
    {
        printf("%d\n%d\n", y, x);
    }
    else
    {
        printf("%d\n%d\n", x, y);
    }

    return 0;
}
